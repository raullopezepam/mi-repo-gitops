apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hipstershop
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/tu-usuario/mi-repo-gitops  # 👈 Reemplaza con tu repo real
    targetRevision: HEAD
    path: apps/hipstershop
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
